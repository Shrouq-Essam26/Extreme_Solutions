<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mywe" >

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="com.example.mywe.core.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar" >
       <activity android:name=".presentation.ui.activities.productDetails.ProductDetailsActivity"/>


        <activity
            android:name=".presentation.ui.activities.intro.IntroActivity"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".presentation.ui.activities.browse.BrowseActivity"
            android:exported="false" >

        </activity>
        <activity
            android:name=".presentation.ui.activities.cart.CartActivity"
            android:exported="false" >

        </activity>
    </application>

</manifest>